apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app-load-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-app-load-postgres
  template:
    metadata:
      labels:
        app: demo-app-load-postgres
    spec:
      containers:
      - name: demo-app-load-postgres
        image: dbazhenov/demo_app_load:0.1.5
        imagePullPolicy: Always
        resources:
          requests:
            memory: "1Gi"
            cpu: "1"
          limits:
            memory: "1Gi"
            cpu: "1"
        envFrom:
        - configMapRef:
            name: demo-app-config
        - secretRef:
            name: demo-app-secret
        env:
        - name: LOAD_MYSQL
          value: "false"
        - name: LOAD_POSTGRES
          value: "true"
        - name: LOAD_MONGODB
          value: "false"
